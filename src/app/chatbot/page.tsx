"use client";

import React from "react";
import Chatbot from "../../components/Chatbot";

const page = (): React.ReactNode => {
  return (
    <div>
      <Chatbot></Chatbot>
    </div>
  );
};

export default page;
